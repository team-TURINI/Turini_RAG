# 필수 — 임베딩 + 생성 LLM
OPENAI_API_KEY=sk-...

# 필수 (최종 파이프라인) — Cohere 리랭커
#   Trial 키는 분당 10회 제한 → 157문항 약 17분. 반복 실행하려면 Production 키 권장
COHERE_API_KEY=...

# 선택 — 인덱스 디렉토리 (기본: ./vectorstores)
# INDEX_DIR=/var/lib/rag/vectorstores
